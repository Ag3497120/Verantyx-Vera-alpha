# この企てで作業する前に

## 実装の前に、必ず索引を引く

```bash
python3.11 -m verantyx.cli index search "<やろうとしていること>"
```

67,145行・**130扉**・89 fork・75通の事前登録・441ページの構想。
この量は人の作業記憶にも、モデルの文脈窓にも入らない。だから
**既にあるものを作り直す事故が実際に繰り返し起きている**:

- 「保留をIDEに出すUI」を作りかけ → 22の承認扉・7の欠け扉・
  `propose_web_evidence`・睡眠モードが既にあった
- 「意味の一般化はできない」と断言 → これまで.pdf に**できている実測**
  (「TypeScriptの代わりにjavascript」を禁止語ゼロで名指し)があった

索引はソースと文書から**その場で導出**する(手書きの一覧は持たない)ので
古くならない。無いものには `UNKNOWN_NOT_FOUND` を返す — 似た名前を
代わりに返さないのは、在ると誤認させるほうが作り直しより高くつくから。

MCP からは `capability_index(query)`。

## 健康を確かめる

```bash
python3.11 -m verantyx.cli doctor          # 保証(G1-G4 / S1-S4)+ 配線
python3.11 experiments/guard/verify_all.py # fork 89 + 測定 50 を45秒で
```

## 守る線(measured、破ると測定が落ちる)

- **測る前に事前登録**(experiments/*/PREREG*.md)。数値は実行結果のみ、
  予想は書かない。外れた仮説はそのまま記録する — 外れの記録が資産
- **束ねず重ねる**。層の間で規則を持ち回らない(2度却下されている)
- **同点は棄権**。決定的な同点崩しは信号を壊す(73.3% → 23.7% の実測)
- **不在と否定を混ぜない**。UNKNOWN_* は型で返す
- **削除しない**。退役は追記、間引きは書庫への移動
- **配置は情報を増やせない**。配置を変えると消える答えは偽物
- 治具は**測るものと同じ経路**で作る(破って3度失敗している)
- 凍結は必ず `rm -f dist/vera-memory && pyinstaller --noconfirm --clean`
  (中身が古いまま「Build complete」と出る罠を実測済み)

## 保証していないこと(直さないと決めた。PREREG5/6 に全文)

自然言語の指示の読解、字面の回避(TODO→FIXME)、日本語の一般語の
required、シェルの完全解釈、対比が書かれていない語の推論、文章の生成。
これらは**規則を足しても閉じない**ことを三重に実測済み
(645/661 が語彙の外 / 頻度に窓なし / コーパスは在庫だけ)。
