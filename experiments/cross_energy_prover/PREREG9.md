# 事前登録9: AC冗長規則の除去 — 「一致」の意味を二重化しない(最後の変分)

日付: 2026-08-20。**この文書を書き終えてから測定を走らせる。**

## 確認8の正直な記録(Y1違反・不採択)と機序

クラスフィールドで基礎(zero_mul等)は埋まり、**基底は閉じるように
なった**。しかし段で両辺が予算切れ: 生トレースで
ml:commute.eq.to_additive が 297/600 発 — **無向AC規則の族(comm /
right_comm / mid_assoc)が、局所減少の順序門をすり抜けて循環する**
(各適用は部分項の印字キーを厳密に減らすが、位置間で材料が動くため
大域では停止しない)。fork 168 の「印字順序は原理的でない」が
手渡し規模で再発した形。

## 思想からの導出

この証明器の「一致」は既に **ac_norm(比較の正規化、規則の外)** が
担っている。AC規則を書き換え規則としても渡すのは、**同じ「一致」の
意味を二つの機構に混ぜる束ね** — 束ねず重ねる、の違反。よって:

## 変更(これ一つだけ)

load_mathlib_context で、**両辺が ac_norm で同型になる規則
(AC冗長規則)を手渡しから除く**。除いた規則は台帳に
「ac_redundant として不搭載」と記録(消えるのではなく、比較器が
既に担っていることの明示)。しきい値なし・決定論。

## 判定

- Z1: 証明集合 ⊇ 確認4の22本(B9含む)
- Z2: P束 ≥ 4/6、mathlib引用つき ≥ 3
- Z3: 不健全 0
- 除外された規則数と残存数を報告

## 停止条件

Z1違反 → **壁**。手渡しの粒度は思想からの導出では解けないと記録し、
操作者に返す(恒久指示 2026-08-20 の取り決めどおり)。
