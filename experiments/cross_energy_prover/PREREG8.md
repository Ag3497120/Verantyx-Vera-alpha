# 事前登録8: クラスフィールドの抽出 — 断片の供給の欠けを埋める

日付: 2026-08-20。**この文書を書き終えてから測定を走らせる。**

## 確認7の正直な記録(X1違反・不採択)

50本全量の手渡しで B9 が三たび退行、P束は 1/6。診断(読み取り専用):
- 敗因は汚染ではなく**部分供給**: add(0,?a)→?a が内側で先に発火して
  両辺の整列を崩し、残る mul(0,c) の還元規則が断片に無い
- zero_mul / mul_zero / zero_add / add_zero / one_mul' 等は mathlib では
  定理でなく**クラスのフィールド**(where 節)— structure_to_lean が
  「クラスのフィールドは構造上不在」と記録していた穴の実害化

## 変更(抽出器に1つ)

build_mathlib_statements.py にクラスフィールドの抽出を追加:
`class X … where` ブロックの `field : ∀ …, lhs = rhs` を定理と同格に
扱い、**同じ決定門**(多項式同一性・格子)を通す。ℕ で偽になる
フィールド(群の逆元等)は文字集合と門が落とす — 許可表は増やさない。

## 確認測定8

確認7と同一(C/R/B + P1..P6、手渡し全量)。断片は v2(フィールド込み)。

## 採択基準

- Y1: 証明集合 ⊇ 確認4の22本(B9含む) — 退行なし
- Y2: P束 ≥ 4/6 証明、うち mathlib 引用つき ≥ 3
- Y3: 不健全 0(主張は全て Lean VERIFIED)
- Y4: 断片 v2 の格子門通過は全数記録(refuted/undecided の内訳)

## 停止条件

Y1 違反 → 供給を「詰まりの記号に触れる規則だけ」に絞った版を最後の
変分として事前登録9で測る。それでも退行するなら**壁**として操作者に
返す(手渡しの粒度という設計問題)。
