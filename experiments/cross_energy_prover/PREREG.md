# 事前登録: 立体十字エネルギー証明器 — 梯子の発明の確認

日付: 2026-08-20。**この文書を書き終えてから run_confirm.py を走らせる。**

## 構想の出所

「これまで.pdf」の中核 — 端に足されるクエリで各ノードのエネルギー比率が
変わり、中心への経路が変わることで推論する — を証明探索に写像した:
詰まった段の等式がクエリ、候補補題が腕、記号被覆×特定性×接地質量が
エネルギー、エネルギーが試行順(経路)を決める。真偽には一切触れない。

## 開発済み(seed なし・決定論、results_dev.json)

D1 app(x,nil)=x、D2 len(app)=add(len,len)、D3 rev(rev(x))=x の3本を
開発中に使用。D3 は補題 rev(app(x, cons(c, nil))) = cons(c, rev(x)) を
**発明して**証明した(探索実験 2026-08-19 で「梯子の段そのものは発明
できない」と実測された穴が、抽象化+列挙+接地淘汰で埋まった)。

## 確認目標(開発に未使用・この文書で固定)

    C1  rev(app(x, y)) = app(rev(y), rev(x))   ← 梯子(app結合律級)が要る
    C2  len(rev(x)) = len(x)                    ← len_app 級の発明が要る
    C3  app(app(x, y), z) = app(x, app(y, z))
    C4  mul(a, add(b, c)) = add(mul(a, b), mul(a, c))  ← 分配律
    回帰 R1..R8 = 探索実験の G1..G8(nat、変数を a,b,c に読み替え)

補題は一切与えない。定義10本(DEFS)のみから。

## 採択基準

- Q1 健全性: 証明したと主張した確認目標・回帰は **全て Lean VERIFIED、
  不健全 0**(探索実験と同じ判定)
- Q2 発明: C1 と C2 が、自動発明の補題 ≥1 本を使って証明される
- Q3 回帰: R1..R8 のうち ≥7(旧実装の 8/8 と比較して報告)
- Q4 淘汰の正直さ: 昇格した全補題を、開発時より大きい接地域
  (N≤4・リスト長≤3)で再検査して REFUTED 0
- 参考記録(採否に使わない): エネルギー無効(列挙順)との対照 —
  証明数・ノード数・発明数。エネルギーの寄与はここで初めて数字になる

## 停止条件

Q1 違反(Lean が落とす主張が1本でも出る)→ 器の欠陥。棚上げして
RESULTS に記録。Q2/Q3 未達 → 採択せず、届いた所までを正直に記録。
