# 事前登録11: リスト署名の mathlib 断片 — 完了基準5点で測る

日付: 2026-08-20。**この文書を書き終えてから測定を走らせる。**
操作者の完了基準(同日): 「実装した」で終わらせず、①正しい証明
②偽の反例つき棄却 ③仮定不足の拒否 ④探索順の不変 ⑤独立検証、を全て測る。

## 作るもの

1. **リスト等式断片** — 文サイドカー(179,641文)から List の等式定理を
   閉じた文法(識別子・++・::・[]・.length・.reverse・+・括弧)で抽出し、
   我々の署名(app/cons/nil/len/rev/add)に写す。
2. **輸入の門(リスト版)**: 多項式同一性はリストでは決定手続きに
   ならないため、二段: (a) 接地検査(反駁のみ — 偽を落とす)
   (b) **規則ごとの Lean 個別検証**(自動翻訳+戦術)— 通らないものは
   輸入しない(undecided は不搭載)。輸入規則の証人は
   verified:lean4:local(この機体で今日 earned)。
3. **目標レベルの反駁門**: prove() の入口で ground_check — 偽の目標は
   REFUTED(反例を名指す)として台帳へ。証明失敗(拒否)と区別する。
4. 手渡しの席・向き(LPO)・AC冗長除去は確認9/10で採択済みの形のまま。

## 測定(5点それぞれ)

- 回帰: C/R/B/P 30目標(確認10と同一)
- ①正しい証明: **B1・B10 が閉じること**(List断片が届けば構造上
  閉じるはず)+ 新リスト束 L1..L4:
    L1 rev(app(app(x, y), z)) = app(rev(z), app(rev(y), rev(x)))
    L2 len(app(x, app(y, z))) = add(len(x), add(len(y), len(z)))
    L3 len(rev(app(x, y))) = add(len(y), len(x))
    L4 rev(rev(app(x, y))) = app(x, y)
- ②偽の棄却: 偽束 F1..F4(**証明されたら即失格**、REFUTED+反例が正):
    F1 app(x, y) = app(y, x)
    F2 rev(x) = x
    F3 len(app(x, y)) = len(x)
    F4 rev(app(x, y)) = app(rev(x), rev(y))
- ③仮定不足の拒否: 真でも偽でもなくこの器で届かない命題は型付き拒否の
  まま台帳へ(証明も反駁もしないこと)。仮説つき List 定理が輸入で
  落ちた数も記録
- ④順序の不変: ml規則列と DEFS の順序を反転した第二走で、全目標の
  verdict(proved/refuted/refused)が一致
- ⑤独立検証: 証明主張は全て Lean VERIFIED・輸入規則も全て個別 VERIFIED

## 採択基準

- A1: 輸入規則 ≥ 8、全て個別 Lean VERIFIED
- A2: B1・B10 証明(全体 30/30)+ L束 ≥ 3/4
- A3: F束 証明 0、REFUTED ≥ 3(反例つき)
- A4: 順序反転で verdict 完全一致
- A5: 不健全 0
- 輸入時の名前↔文の突合を無作為5本で目視(誤帰属の検査)

## 停止条件

A3違反(偽が証明される)→ 器の欠陥、即棚上げ・原因究明が最優先。
A4違反 → 順序依存の残り火 — 機序を特定して単独の事前登録。
その他未達 → 届いた所まで記録。
