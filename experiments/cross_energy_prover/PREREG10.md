# 事前登録10: 手渡しの席を全検査に統一(直接検査にも供給)

日付: 2026-08-20。**この文書を書き終えてから測定を走らせる。**

## 診断(確認9の残・読み取り専用)

P3(二項展開)は ml 文脈を**直接検査**に渡すだけで両辺が同一の正規形に
落ちる(実測で確認)。PREREG7 は供給先を基底/段/詰まりと書き、直接検査を
外していた — 意図した制限ではなく字義の穴。同じ規則集合・同じ門で、
検査によって文脈が違うのは「同じ門を全扉で」(単一ソブリン)に反する。

## 変更(これ一つだけ)

prove() の直接検査にも ml 文脈を供給する(引用も同様に記録)。
供給規則集合・向き・門は確認9と同一。

## 判定

- W1: P3 が証明される
- W2: 確認9の27本に退行なし
- W3: 不健全0

## 停止条件

W2違反 → 棄却して記録(直接検査は文脈なしに戻す)。
