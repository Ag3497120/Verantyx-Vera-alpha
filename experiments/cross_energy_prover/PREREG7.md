# 事前登録7: mathlib断片の手渡し — 在庫は投票せず、詰まりの文脈にだけ届く

日付: 2026-08-20。**この文書を書き終えてから run_confirm7.py を走らせる。**

## 思想からの導出(操作者指示 2026-08-20: 思想第一)

持ち越し(規則の共有プール)は2回棄却された — あれは**束ね**だった。
守る線「票に入れるか、手渡すか」に従い、mathlib断片50本は:

- **昇格しない・蓄積しない** — グローバル規則在庫に一切入らない
- **手渡し** — 帰納の基底/段の検査と詰まりの閉包検査の文脈にだけ、
  IH と同じ席で供給される(IH = 「この文脈でだけ使ってよい規則」の
  既存の型)
- **手渡しの門** — 向きは LPO が決める(normal/reversed は無条件、
  可換律など比較不能は既存の順序門つき)。LPO は昇格用としては
  棄却されたが、ここは用途が違う: 自前の発明ではなく Lean 証人つきの
  外部在庫であり、向き付けは適用の安全化のみ
- **引用** — 発火した断片規則の mathlib 名を台帳に記録(参照が発明に
  先行する: 文脈に在庫が居れば、閉じた詰まりは発明を起こさない)

## 確認測定7

- 回帰: C1..C4 / R1..R8 / B1..B12(確認4と同一、独立 Prover)
- 新バッテリー P1..P6(この文書で固定、多項式の展開・因数分解):
    P1 add(mul(a,b), mul(a,c)) = mul(a, add(b,c))
    P2 mul(add(a,b), c) = add(mul(a,c), mul(b,c))
    P3 mul(add(a,b), add(a,b)) = add(mul(a,a), add(mul(a,b), add(mul(b,a), mul(b,b))))
    P4 mul(a, add(b, add(c,d))) = add(mul(a,b), add(mul(a,c), mul(a,d)))
    P5 mul(mul(a, add(b,c)), d) = add(mul(mul(a,b),d), mul(mul(a,c),d))
    P6 add(mul(add(a,b),c), mul(add(a,b),d)) = mul(add(a,b), add(c,d))
  Lean 治具6本は事前検証済み(6/6 VERIFIED — 検査器を後から直さない)。

## 採択基準

- X1 回帰: 確認4の証明集合(22本)と同一以上・不健全0
- X2 引用: P束のうち mathlib 引用つきで閉じた目標 ≥ 3
- X3 P束の誤答0(証明したものは全て Lean VERIFIED)
- X4 台帳: 引用(mathlib名)が proof_ledger に実在する
- 実行時間と「発明が参照に置き換わった数」は報告

## 停止条件

X1違反(退行)→ 手渡しでも汚染するなら、供給の範囲(50本全部 vs 詰まり
の記号に触れるものだけ)が変数 — 絞った版を**新しい事前登録**で。
X2未達 → 断片が載る目標が無いということ。正直に記録して締める。
