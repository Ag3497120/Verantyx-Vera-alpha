# 事前登録: mathlib の文構造つき再抽出 — 名前だけの店に文を取り戻す

日付: 2026-08-20。**この文書を書き終えてから抽出を走らせる。**

## 現状(実測)

mathlib_store.json は 名前→識別子トークン+証人 のみ。定理の**文**が
無いので、75,919本の検証済み定理は「照会」にしか使えず、証明の
**補題在庫**として使えない(structure_to_lean が「再抽出が要る」と
記録した欠落)。ソースは ~/Projects/vera-corpus/corpora/mathlib4 に実在。

## 作るもの

1. **文サイドカー** mathlib_statements.json — 宣言名 → {文(:= の前の
   型部)、束縛変数、ファイル、行}。連邦に入れない(独立ソブリン、票なし)。
2. **等式断片** — 文が「束縛が全て ℕ/Nat で、本体が閉じた文字集合
   (識別子・数字・+ * - ・括弧)の等式 lhs = rhs」のもの。書き換え核の
   パターン規則(束縛変数→?v)に変換して格納。**mathlib の証人
   (verified:lean4)を持つ宣言のみ**規則候補として印をつける。
3. **語→宣言の転置索引** — 詰まりの記号から候補補題を引く(逆方向被覆と
   同じ形)。

## 判定

- W1 被覆: 文が取れた宣言 ≥ 旧店の核数(75,171)の 95%
- W2 突合: 無作為20宣言の(名前, 文)をソース原文と目視突合して 20/20
- W3 等式断片: Nat等式規則 ≥ 50本、全て kernel の parse_term を通る
- W4 実用デモ: 証明器が発明した既知補題(add(0,a)=a 級)に対し、断片
  検索が対応する mathlib 宣言(Nat.zero_add 級、証人つき)を返す ≥ 3例
- 断片規則を証明器の在庫に入れる実験は**次の事前登録**(今回は抽出と
  索引まで — 持ち越し汚染の教訓があるため、在庫投入は単独で測る)

## 停止条件

W1/W2 違反 → 抽出器の欠陥、修理してから再測(基準は動かさない)。
