# 実測結果: 自分のハーネスを自分で測った(5点全合格)

日付: 2026-08-21。事前登録 PREREG.md、生データ results.json、2,011秒。
構想「行動の蓄積で覚えて発明する」の完結点 — 実在の行動ログ(証明器の
作業記録)と、無料で決定的な審判(Lean + 48目標)。

## Q1 作業ログはハーネス項に写った(未写像 0)

    hact(0)                              35  直接検査だけで閉じた
    hseq(hact(1), hact(2))                2  基底 → 段
    hseq(hseq(hact(1), hact(2)), hact(3)) 2  + 発明した補題
    hseq(hact(0), hact(4))                2  + 条件の放電
    未写像                                 0

**証明器の振る舞いは、41目標中35が「直接検査だけ」**だった。手渡し在庫
(mathlib/core 断片)が届いた後、この目標集合の大半は発明も帰納も要らない。

## 変分7本の判定(単一変更のみ・全て分類済み・黙った採択0)

| 変分 | 変更 | 証明 | 判定 |
|---|---|---|---|
| wave1 | 腕 6→1 | 41 (+0) | 棄権 |
| wave12 | 腕 6→12 | 41 (+0) | 棄権 |
| depth2 | 深さ 3→2 | 41 (+0) | 棄権 |
| depth4 | 深さ 3→4 | 41 (+0) | 棄権 |
| budget500 | 予算 4000→500 | 41 (+0) | 棄権 |
| **no_handoff** | 手渡し在庫を外す | **33 (−8)** | **害** |
| no_energy | エネルギー無効 | 41 (+0) | 棄権 |

    Q2 ✓ 害が検出された(no_handoff)
    Q3 ✓ 全変分が採択/棄権/害に分類(黙った採択 0)
    Q4 ✓ 目標順を反転しても採否が一致
    Q5 ✓ 不健全 0(採択が無いので空、反駁は全変分で 7/7 維持)

## 読み — 静かな結果が、一番はっきりした証拠だった

採択は**ゼロ**。7つの把手のうち6つは、動かしても証明数が1つも動かない。
唯一効いたのは**手渡し在庫**で、外すと8目標を失う。

つまりこの証明器の力は、**探索の広さ(腕)・深さ・予算・順序づけでは
なく、参照できる在庫の有無**でほぼ決まっている。「参照は発明より速い」
(PREREG11)の、把手の側からの裏づけ。

**エネルギーの寄与ゼロ**は確認14に続き二度目の測定で、これで
「エネルギーは順序を決めるが、この規模の目標では結果を変えない」が
二つの独立した測り方で一致した。構想の中心にある機構について、
不利な結果を二度とも記録している。

自分の把手を回して「効かない」と6回言えたことが、この器官の価値。
DSPy/Optuna 系はここで必ず「最良の設定」を返す。

## 台帳

self_harness_facts.json に7件、文脈鍵 `prover@prover_goals48`、
証人 `verified:run:prover@prover_goals48@2026-08-21`、
各行に「この変分の成功が何を立証しようとしたか」つき。
