__pycache__/
*.pyc
*.egg-info/
build/
dist/
vera_store.json
*.local.json
.DS_Store

# Verantyx JCross Vault (local only)
.openclaw/jcross_vault/

.venv*/
*.egg-info/

# Truncated scratch file, unparseable and referenced nowhere; kept on disk
# only because it is the user's to decide about.
verantyx/dialogue_context.py

# Fetched corpora. Manifests are committed; the documents are not — they are
# third-party government publications, and a frozen copy would quietly diverge
# from what the publisher is actually serving.
corpora/*/
!corpora/*.json

# Built artifacts staged for publication — 150MB, and reproducible from the
# manifests in corpora/ by build_ja + build_en + export_sqlite.
hf/model/vera.db
hf/model/writer.json
hf/space_static/vera_web.db
hf/space_static/writer.json
hf/space_static/verantyx.zip
